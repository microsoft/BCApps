name: 'Validate Country Projects'

# Validates that country project settings.json files have correct W1 app dependencies

on:
  push:
    branches: [ "main", "releases/*" ]
    paths:
      - "src/Apps/**/app.json"
      - "build/projects.json"
      - "build/groups.json"
      - "build/projects/Apps */.AL-Go/settings.json"
      - "build/scripts/Update-CountryProjectSettings.ps1"
      - ".github/workflows/ValidateCountryProjects.yaml"
  pull_request:

permissions:
  contents: read

jobs:
  ValidateCountrySettings:
    name: Validate Country Settings
    runs-on: windows-latest
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@95d9a5deda9de15063e7595e9719c11c38c90ae2 # v2.13.2
        with:
          egress-policy: audit

      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1

      - name: Validate Country Project Settings
        shell: pwsh
        run: |
          $result = & "${{ github.workspace }}/build/scripts/Update-CountryProjectSettings.ps1" -Validate
          if ($result -eq $false) {
            Write-Host "::error::Country project settings are out of date. Run 'build/scripts/Update-CountryProjectSettings.ps1' and commit the changes."
            exit 1
          }
