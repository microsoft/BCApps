name: 'Verify App Changes'

on:
  pull_request:
    branches: [ 'main', 'releases/*']

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  VerifyAppChanges:
    runs-on: windows-latest
    name: Verify App Changes
    steps:
      - name: checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0

      - uses: microsoft/BCAppsPrivate/.github/actions/TestPreprocessorSymbols@main  # TODO: update to microsoft/BCApps

      - uses: microsoft/BCAppsPrivate/.github/actions/TestObjectIdsAndManifests@main  # TODO: update to microsoft/BCApps

      - uses: microsoft/BCAppsPrivate/.github/actions/VerifyMiappSync@main # TODO: update to microsoft/BCApps/.github/actions/VerifyMiappSync@main
