{
    "name": "Ruleset for Shopify connector",
    "description": "Temporary ruleset for Shopify connector to allow migration to BcApps repo",
    "includedRuleSets": [
		{
			"action": "Default",
			"path": "./ruleset.json"
		}
	],
    "rules": [
        {
            "justification": "Temporary disabling until translation file is updated",
            "id": "AS0087",
            "action": "None"
        }
    ]
}