# cost: 10
{"query":"mutation { webhookSubscriptionCreate( topic: {{WebhookTopic}} webhookSubscription: {callbackUrl: \"{{NotificationUrl}}\", format: JSON} ) { userErrors { field message } webhookSubscription { id } } }"}