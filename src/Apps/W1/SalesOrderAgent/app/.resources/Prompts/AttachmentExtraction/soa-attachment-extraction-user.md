Extract all Sales Order Agent-relevant information from the complete attached file.

Return one JSON object shaped like the following filled example. The example comes from a different document.
Never copy its values. Keep its property names when they fit. Add extra properties whenever the attached file contains a relevant fact that no shown property expresses. Omit unavailable information instead of inventing it. Only schema is mandatory.

%1
